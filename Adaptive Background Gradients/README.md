
  # Adaptive Background Gradients

  This is a code bundle for Adaptive Background Gradients. The original project is available at https://www.figma.com/design/Qui5jeRQ4pMcLYQIKMe362/Adaptive-Background-Gradients.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  